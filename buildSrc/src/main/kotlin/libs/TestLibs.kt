object TestLibs {
    const val junit = "junit:junit:${TestLibsVersions.junit}"
    const val mockk = "io.mockk:mockk-android:${TestLibsVersions.mockk}"
    const val coroutines = "org.jetbrains.kotlinx:kotlinx-coroutines-test:${AppLibsVersions.coroutines}"
}

object TestLibsVersions {
    const val junit = "4.13.2"
    const val mockk = "1.13.4"
}